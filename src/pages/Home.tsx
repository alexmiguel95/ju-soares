import React from 'react';
import styled from 'styled-components';

const Home = () => {
    return (
        <StyledContainer>
            
        </StyledContainer>
    );
}

const StyledContainer = styled.div`

`;

export default Home;
